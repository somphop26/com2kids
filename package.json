{
  "name": "com2kids",
  "version": "1.0.0",
  "description": "Com2kids website",
  "main": "index.js",
  "scripts": {
    "deploy": "rimraf public && hugo --minify && gh-pages -d public",
    "build": "rimraf public && hugo --minify",
    "update": "git add -A && git commit -m 'update' && git push"
  },
  "author": "Paiboon Auengkongkatong",
  "license": "ISC",
  "homepage": "https://paiboon15721.github.io/com2kids",
  "dependencies": {
    "@fortawesome/fontawesome-free": "^5.10.2",
    "bootstrap": "^4.3.1",
    "gh-pages": "^2.1.1",
    "postcss-cli": "^6.1.3",
    "rimraf": "^3.0.0"
  }
}
